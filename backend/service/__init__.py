"""Backend services (repository extraction, etc.).

Service modules contain business logic without HTTP concerns—e.g. safe zip
extraction and chunking in ``repository_service``. Controllers call into here.
"""
