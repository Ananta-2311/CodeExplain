"""HTTP API route modules (FastAPI routers).

Each submodule defines an ``APIRouter`` mounted from ``main`` (explain,
visualize, history, repositories, etc.). Import this package to register
side effects only when needed; routers are normally imported explicitly.
"""

__all__ = []


